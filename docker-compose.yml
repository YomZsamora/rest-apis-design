version: '3.8'

services:

  rest-apis-design:
    build: .
    command: sh -c "npm run dev"
    ports:
      - "3024:3000"
    env_file:
      - ./.env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules 
    networks:
      - dev-infra

networks:
  dev-infra:
    external: true